Aggiunta utente
<%= form_tag(add_user_sp_bibliographies_path, method:'post') do %>

<%= select_tag(:user_id, options_for_select(User.sp_user_select,
      params[:user_id]), prompt:'Utente da aggiungere') %>


<%= submit_tag('Aggiungi utente') %>
<% end %>
