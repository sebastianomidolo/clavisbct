<%= content_tag(:pre, "Impostazioni PDF per #{link_to(@d_objects_folder.name, d_objects_folder_path)}".html_safe) %>

<%=
  if @d_objects_folder.id==0
    render partial:'pdf_params_custom'
  else
    render partial:'pdf_params'
  end
%>
