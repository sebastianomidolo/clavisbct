<script>
    function add_library(selectObject) {
        v=selectObject;

	var library_id=jQuery('#library_id',selectObject.parentElement).val();
	var id=jQuery('#serial_list_id').val();
        var url='/serial_lists/' + id + '/add_library?library_id=' + library_id;
    url=encodeURI(url);
    jQuery('#xyz').html(url);
    
    }
</script>

Pagina in progettazione - non utilizzabile

<%= hidden_field_tag('serial_list_id', @serial_list.id) %>

<%= content_tag(:div, 'x', {id:'xyz'}) %>

<%
  select_tag(:library_id, options_for_select(ClavisLibrary.library_select('lk')),
    prompt:'Aggiungi una biblioteca',
    :onchange=>'add_library(this);', include_blank:false)
%>

<%= content_tag(:div, serial_libraries_list(@serial_libraries)) %>


