<% title,content=clavis_manifestation_show_attachments(@clavis_manifestation,params,request,@dng_session) %>
<% if !title.nil? %>
jQuery("#attachments").html('<%= content %>');
jQuery('a','#attachments_tab').text('<%= title %>');
jQuery('#attachments_tab').fadeIn(1000);
<% end %>
