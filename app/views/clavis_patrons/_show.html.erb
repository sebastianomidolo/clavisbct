<style>
  .user_message {
  color: white;
  margin-left: 0px;
  background-color: black;
  border: 3px solid gray;
  padding-left: 8px;
  font-size: 100%;
  font-weight: bold;
  width: 90%;
  height: 50px;
  }
</style>

<%= content_tag(:h3, "#{@patron.lettore} <b>#{@patron.to_label}</b> (#{link_to(@patron.barcode, @patron.clavis_url(:newloan))})".html_safe) %>

<%= render :partial => 'item_search' %>

<%= content_tag(:div, clavis_patron_show(@patron,flash[:notice]), id:'clavisbct_unique_id') %>

