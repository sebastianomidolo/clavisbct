<%= will_paginate @clavis_items, renderer: BootstrapPagination::Rails %>
<%=
   if @order_by=='dewey_collocation'
     content_tag(:p, link_to("[passa a ordinamento per collocazione]", ricollocazioni_clavis_items_path))
   else
     content_tag(:p, link_to("[passa a ordinamento per Dewey]", ricollocazioni_clavis_items_path(:sort=>'dewey')))
   end
%>
<%= clavis_items_ricollocazioni(@clavis_items) %>

