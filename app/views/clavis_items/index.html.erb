<%= render :partial => 'searchform' if params[:nosearchform].blank? %>

<% content_tag(:div, @sql_conditions, class: 'panel panel-success') if current_user.email=='seba' %>
<%= content_tag(:div, clavis_items_missing_numbers(@clavis_item.collocation)) %>
<%= will_paginate @clavis_items, renderer: BootstrapPagination::Rails %>
<%= clavis_items_shortlist(@clavis_items) %>

<%= content_tag(:pre, params[:sql]) if !params[:sql].blank? %>
